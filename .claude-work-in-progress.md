# 進行中の作業 — 候補 D: NUMERIC/DECIMAL 型 + DATE/TIME 型

## 状況
**実装開始済み、途中で中断**

## 完了した部分
- ✅ `base.py` に型変換フック追加: `from_db()` / `to_db()` メソッド（60行目付近）

## 残りのタスク

### 1. 新しい列型クラスの実装（`base.py`）
以下の 4 つのクラスを追加:

```python
from decimal import Decimal

class DecimalColumn(Column[Decimal]):
    """NUMERIC(max_digits, decimal_places) 型。精度を損なわない。"""
    sql_type = "NUMERIC"
    
    def __init__(self, max_digits: int, decimal_places: int, **kwargs):
        super().__init__(**kwargs)
        self.max_digits = max_digits
        self.decimal_places = decimal_places
    
    def from_db(self, value: Any) -> Decimal | None:
        if value is None:
            return None
        return Decimal(str(value))
    
    def to_db(self, value: Any) -> str | None:
        if value is None:
            return None
        return str(Decimal(value))
    
    def ddl_fragment(self) -> str:
        parts = [f"NUMERIC({self.max_digits},{self.decimal_places})"]
        if self.primary_key:
            parts.append("PRIMARY KEY")
        if not self.nullable and not self.primary_key:
            parts.append("NOT NULL")
        if self.unique:
            parts.append("UNIQUE")
        return " ".join(parts)


class DateColumn(Column[datetime.date]):
    """DATE 型。時刻なし。"""
    sql_type = "DATE"
    
    def from_db(self, value: Any) -> datetime.date | None:
        if value is None:
            return None
        if isinstance(value, datetime.date):
            return value
        return datetime.datetime.fromisoformat(value).date()
    
    def to_db(self, value: Any) -> str | None:
        if value is None:
            return None
        if isinstance(value, datetime.date):
            return value.isoformat()
        return value


class TimeColumn(Column[datetime.time]):
    """TIME 型。日付なし。"""
    sql_type = "TIME"
    
    def from_db(self, value: Any) -> datetime.time | None:
        if value is None:
            return None
        if isinstance(value, datetime.time):
            return value
        return datetime.datetime.fromisoformat(f"2000-01-01T{value}").time()
    
    def to_db(self, value: Any) -> str | None:
        if value is None:
            return None
        if isinstance(value, datetime.time):
            return value.isoformat()
        return str(value)
```

### 2. `__init__.py` の Engine で型変換を統合
- `_fetch()` / `_fetchval()` の戻り値を `from_db()` で変換
- `_insert()` / `_update()` の引数を `to_db()` で変換

SQLite / asyncpg / aiomysql 各エンジンで実装が必要な個所：
- `_fetch()` 内の行辞書値を各カラムの `from_db()` に通す
- `_insert()` / `_update()` でパラメータを `to_db()` に通す

### 3. テストの作成
- `tests/test_decimal_date_time.py` を新規作成
  - DecimalColumn の精度テスト（Decimal 値の等値比較）
  - DateColumn の日付入出力テスト
  - TimeColumn の時刻入出力テスト
  - 各型の NULL 処理
  - ORM クエリ（filter / create / update）での動作確認

### 4. TODO 更新
- `[ ] NUMERIC / DECIMAL 型` → `[x]`
- `[ ] DATE 型 / TIME 型` → `[x]`

### 5. コミット
実装完了後、以下メッセージでコミット:
```
Add NUMERIC, DATE, TIME column types with DB-Python conversion

columns/base.py:
- Add from_db() / to_db() conversion hooks to Column base class
- DecimalColumn: NUMERIC(max_digits,decimal_places) with Decimal conversion
- DateColumn: DATE type, datetime.date conversion
- TimeColumn: TIME type, datetime.time conversion

__init__.py:
- Wire up from_db() in _fetch()/_fetchval() for result hydration
- Wire up to_db() in _insert()/_update() for parameter binding

Tests: Decimal precision, date/time round-trip, NULL handling, ORM integration

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

## 進捗
- 全体: 5% (型変換フック追加のみ)

## 次のステップ
次のセッションで `base.py` に 4 つの新しい列型クラスを追加することから開始。
