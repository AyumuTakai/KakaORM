# 進行中の作業

## 状況
**完了** — NUMERIC/DATE/TIME 型の実装が全て完了。

## 完了した作業
- ✅ `base.py` に型変換フック `from_db()` / `to_db()` 追加
- ✅ `types.py` に `DecimalColumn` / `DateColumn` / `TimeColumn` 追加
- ✅ `base.py` の `ColumnMeta` 比較演算子で `to_db()` を適用（WHERE 句パラメータ変換）
- ✅ `query.py` の `_hydrate()` で `from_db()` を適用（DB行 → Modelインスタンス変換）
- ✅ `Engine._insert()` / `_update()` / `_bulk_insert()` で `to_db()` を適用
- ✅ `AioSQLiteEngine.create_table` に DATE/TIME → TEXT マッピング追加
- ✅ `kakaorm/__init__.py` に新型をエクスポート追加
- ✅ テスト `tests/test_decimal_date_time.py` 作成（12件全通過、合計174件全通過）
- ✅ コミット済み: `2d88a70`

## 次の候補タスク
特になし。TODO.md で未実装の項目を確認してください。
